logging:
  level:
    "[org.springframework.cloud.gateway]": TRACE
spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
